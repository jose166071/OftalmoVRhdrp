using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PatientDat : ScriptableObject
{/*
    private string name;
    private float age;
    private int id;
    private string _fixMeth = "Eye Tracking";
    private string _fixTarget = "central";
    private string _stimType = "III, White";
    private string _background = "31.5 ASB";
    private string strategy = "SITA-Fast";
    private string Date;
    private string Time;
   
    public void Init(string name, float age, int id)
    {
        this.name = name;
        this.age = age;
        this.id = id;
        this.Date = DateTime.Now.ToString("d/M/yyyy");
        this.Time = DateTime.Now.ToString("h:mm:ss tt");
    }

    public static PatientDat CreateInstance(string name, int age, int id)
    {
        var data = ScriptableObject.CreateInstance<PatientDat>();
        data.Init(name, age, id);
        return data;
    }
    */
}
